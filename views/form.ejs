<!DOCTYPE html>
<html lang="en">
<head>
<%- include('partials/header') %> <!-- Include the header -->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= assignment ? 'Edit' : 'Add' %> Assignment</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
  <h1><%= assignment ? 'Edit' : 'Add' %> Assignment</h1>

  <form action="<%= assignment ? '/assignments/' + assignment._id : '/assignments/add' %>" method="POST">
    <label for="title">Title:</label>
    <input type="text" id="title" name="title" value="<%= assignment ? assignment.title : '' %>" required>
    <br>
    <label for="subject">Subject:</label>
    <input type="text" id="subject" name="subject" value="<%= assignment ? assignment.subject : '' %>" required>
    <br>
    <label for="dueDate">Due Date:</label>
    <input type="date" id="dueDate" name="dueDate" value="<%= assignment ? assignment.dueDate.toISOString().split('T')[0] : '' %>" required>
    <br>
    <label for="status">Status:</label>
    <select id="status" name="status">
      <option value="Not Started" <%= assignment && assignment.status === 'Not Started' ? 'selected' : '' %>>Not Started</option>
      <option value="In Progress" <%= assignment && assignment.status === 'In Progress' ? 'selected' : '' %>>In Progress</option>
      <option value="Completed" <%= assignment && assignment.status === 'Completed' ? 'selected' : '' %>>Completed</option>
    </select>
    <br>
    <label for="priority">Priority:</label>
    <select id="priority" name="priority">
      <option value="Low" <%= assignment && assignment.priority === 'Low' ? 'selected' : '' %>>Low</option>
      <option value="Medium" <%= assignment && assignment.priority === 'Medium' ? 'selected' : '' %>>Medium</option>
      <option value="High" <%= assignment && assignment.priority === 'High' ? 'selected' : '' %>>High</option>
    </select>
    <br>
    <button type="submit"><%= assignment ? 'Update' : 'Add' %> Assignment</button>
  </form>
  <%- include('partials/footer') %> <!-- Include the footer -->
</body>
</html>
